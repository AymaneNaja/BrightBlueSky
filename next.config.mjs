/** @type {import('next').NextConfig} */
const nextConfig = {
        images: {
          domains: ['localhost','shuffle.dev','drive.google.com','lh3.googleusercontent.com'],
        },
      }

export default nextConfig;
